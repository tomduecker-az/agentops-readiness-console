[project]
name = "readiness-core"
version = "0.1.0"
description = "Core models and blueprint logic for AgentOps Readiness Console"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["setuptools>=68.0.0"]
build-backend = "setuptools.build_meta"